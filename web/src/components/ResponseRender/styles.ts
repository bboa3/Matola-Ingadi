import styled from 'styled-components';

export const EventScheduleResponse = styled.span`
  font-size: 1.6rem;
  margin-bottom: 0.6rem;
  
  display: flex;
  justify-content: center;
  align-items: center;
`;