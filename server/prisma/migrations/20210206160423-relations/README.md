# Migration `20210206160423-relations`

This migration has been generated by Arlindo jose at 2/6/2021, 6:04:24 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "users" (
    "id" TEXT NOT NULL,
    "name" TEXT,
    "email" TEXT NOT NULL,
    "phone_number" TEXT,
    "token_version" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "user_events" (
    "date_of_event" TEXT NOT NULL,
    "userId" TEXT NOT NULL,
    "eventId" TEXT NOT NULL,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("userId","eventId")
)

CREATE TABLE "events" (
    "id" TEXT NOT NULL,
    "type" TEXT NOT NULL,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "event_histories" (
    "id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL,
    "eventsId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "events_photos" (
"id" SERIAL,
    "path" TEXT NOT NULL,
    "event_history_id" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "accessories" (
    "id" TEXT NOT NULL,
    "type" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "accessories_photos" (
"id" SERIAL,
    "path" TEXT NOT NULL,
    "accessory_id" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "users.email_unique" ON "users"("email")

CREATE UNIQUE INDEX "events.type_unique" ON "events"("type")

CREATE UNIQUE INDEX "accessories.type_unique" ON "accessories"("type")

ALTER TABLE "user_events" ADD FOREIGN KEY("userId")REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "user_events" ADD FOREIGN KEY("eventId")REFERENCES "events"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "event_histories" ADD FOREIGN KEY("eventsId")REFERENCES "events"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "events_photos" ADD FOREIGN KEY("event_history_id")REFERENCES "event_histories"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "accessories_photos" ADD FOREIGN KEY("accessory_id")REFERENCES "accessories"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20210206160423-relations
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,71 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model users {
+  id            String   @id @default(uuid())
+  name          String?
+  email         String   @unique
+  phone_number  String?
+  events        user_events[]        
+  token_version Int      @default(0)
+  created_at    DateTime @default(now()) 
+  updated_at    DateTime @updatedAt
+}
+
+model user_events {
+  date_of_event String
+  user          users @relation(fields: [userId], references: [id])
+  userId        String
+  event         events   @relation(fields: [eventId], references: [id])
+  eventId       String
+  created_at    DateTime @default(now()) 
+  updated_at    DateTime @updatedAt
+  @@id([userId, eventId])
+}
+
+model events {
+  id                String   @id @default(uuid())
+  type              String   @unique
+  users             user_events[]  
+  event_histories   event_histories[]
+  created_at        DateTime @default(now()) 
+  updated_at        DateTime @updatedAt
+}
+
+model event_histories {
+  id          String   @id @default(uuid())
+  title       String
+  description String
+  photos      events_photos[]
+  created_at  DateTime @default(now())
+  updated_at  DateTime @updatedAt
+}
+
+model events_photos {
+  id                  Int   @id @default(autoincrement())
+  path                String
+  event_histories     event_histories @relation(fields: [event_history_id], references: [id])
+  event_history_id   String
+}
+
+model accessories {
+  id          String   @id @default(uuid())
+  type        String   @unique
+  description String
+  photos      accessories_photos[]
+  created_at  DateTime @default(now())
+  updated_at  DateTime @updatedAt  
+}
+
+model accessories_photos {
+  id           Int         @id @default(autoincrement())
+  path         String
+  accssories   accessories @relation(fields: [accessory_id], references: [id])
+  accessory_id String
+}
```


